# NASDX 多智能体分析系统依赖
# pip install -r requirements_nasdx.txt

# 数据获取（已有）
akshare>=1.16.87
mootdx>=0.11.7
tdxrs>=0.6.5,<0.7.0
pandas>=2.2.3
numpy>=2.0.0
requests>=2.32.0

# AI 调用
openai>=1.66.5        # OpenAI 兼容接口（DeepSeek/Qwen/GPT-4o）
tenacity>=8.1.0,<9.0.0 # mootdx 依赖 tenacity<9，保持通达信 fallback 兼容

# Web UI
streamlit>=1.59.2,<1.60.0  # 修复旧版 sidebar 主题空颜色控制台警告

# 数据结构
pydantic>=2.10.6      # 数据验证

# 配置
tomli>=2.4.1          # TOML 解析（Python 3.11 内置 tomllib 也可）

# 可选：美化输出
rich>=13.7.1          # 终端彩色输出
