# CONTEXT

- 当前（2026-08-06 巡检 run 25）：#72 安全基线已落地（P1 Security），commit 已推 `origin/master`，Issue **保持开放**（owner 决策项未满足，不自动关闭）。根因：仓库仅 secret-scan 门禁（#71），缺 Dependabot/CodeQL/分支保护三层。交付：`.github/dependabot.yml`（github-actions+pip，周一并忽略 pip/uv 工具链）、`.github/workflows/codeql.yml`（SAST security-and-quality，SHA 固定、最小权限）、`.github/workflows/windows-desktop.yml`（顶层 contents:read + concurrency + checkout/setup-python SHA 固定 + persist-credentials:false）、`.github/rulesets/master-baseline.json`（deletion+non_fast_forward 已通过 API 生效 id 20492451；PR-review+required_status_checks 列 owner 决策未启用）、`SECURITY.md`、`README.md` 补 CI 门禁说明、测试 `tests/test_github_security_baseline_contracts.py`（23）+ `tests/test_desktop_ci_contracts.py`（SHA 固定断言）。API 侧已确认：Dependabot alerts+security updates 启用、ruleset #20492451 active。测试：github_security_baseline 23/23、desktop_ci 2/2、密钥扫描 passed=1 failed=0。限制（保持开放）：① 完整 master ruleset 切 PR-only 属 owner 决定，启用会阻断当前直推流；② secret_scanning 的 non-provider patterns + validity checks 需仓库 admin 在 Web 端开（gh token 权限不足无法 API 开启）。剩余开放：#61（待 PR #54 合并，勿动）、#12（P0 服务端轮换密钥，勿代码动）、#73/#34（性能，同源可一并收口）。
- 历史（2026-08-06 巡检 run 24）：#71 已修复（P1 Security），commit `b4a3e9a` 已推 `origin/master`，Issue 由 commit 关键字自动关闭（已回帖，comment-5197755510）。根因：必需门禁只有一条 `sk-[A-Za-z0-9_-]{20,}` 正则 + 只扫**当前树**，且 `run_security_checks.py` 与 `run_final_audit.py` 各有一份同样的副本；导致 GitHub PAT/AWS/私钥/Slack/JWT 全漏检，且"先提交密钥、后删除"的历史泄露永远不会让 CI 失败。新增共享模块 `nasdx/secret_scan.py`（15 条多 Provider 规则 + Shannon 熵门 + placeholder 过滤 + 自引用防护；`Finding.redacted()` 只吐 `rule/path/line/fingerprint`，fingerprint=`sha256(secret)[:16]`，CI 日志不二次泄露；`scan_history()` 走 `git rev-list --objects --all` + `git cat-file --batch` 按 blob SHA 去重扫全部可达对象）。新增可审计 allowlist `security/secret_scan_allowlist.toml`（当前 0 条豁免；`load_allowlist()` **拒绝**裸 rule 豁免/通配路径/缺 reason/未知 rule id，豁免必须精确到 path 或 fingerprint）。新增 `.github/workflows/security.yml` 三级门禁（pull_request + push + 每周 schedule + dispatch，`fetch-depth: 0`、`permissions: contents: read`，跑 `--skip-optional --history`；actions pin 到 commit SHA，gitleaks 固定 v8.30.1 且安装后 `sha256sum` 校验）+ `.gitleaks.toml`（`useDefault=true`，allowlist 只窄化 3 个 fixture/示例文件，不禁用任何规则）。关键决定：测试里的假凭据一律**运行时用 `+` 拼接片段**而不是写字面量——否则仓库自带 secret scan 会命中自己的测试文件，比走 allowlist 豁免更干净。测试 `tests/test_secret_scan_contracts.py` **26 新增** + `test_security_checks_contracts` 改断言脱敏输出，两模块共 31/31；既有 delivery/CI/release 契约 15/15 无回归；`--history` 扫 673 blobs 全清。`run_final_audit.py` 仍是 21 pass / 1 fail，该 fail 是既有 README 文档漂移（缺 `未来情景推演`/`nasdx_history.db`/`task_id`/`账户流水`/`风险画像`，属 #68/#74 文档尾巴），已用 HEAD 版脚本跑基线确认结果逐位一致、非本次引入。遗留：GitHub non-provider patterns / validity checks 需仓库 admin 在 Web 端开，代码侧无法提交。CI 收敛：`b4a3e9a` 的 Security Scan 因 gitleaks 命中 4 处**已提交进 git 历史**的测试 fixture（#66/#63/#62 隐私测试故意喂给脱敏函数、且已提交进历史的假 `api_key=`）报错；新增 commit `5fbaac4` 补 `.gitleaksignore`（4 条精确 finding 级豁免 `<commit>:<path>:<rule>:<line>` 各带 `# reason:`，只豁免 tests/ 下；反向验证同文件换一行再提交新 token 仍被拦=非路径级豁免）、`security.yml` 显式传 `--gitleaks-ignore-path .gitleaksignore`、`run_final_audit.py` 加对应断言、`tests/test_secret_scan_contracts.py` 新增 GitleaksIgnoreTest 锁格式（契约 35/35）。**最终落地 commit 是 `5fbaac4`**（主回帖引用的 `b4a3e9a` 仅第一轮修复、其 Security Scan 后被 `5fbaac4` 覆盖），其 Security Scan（secret-scan）与 Windows Desktop Checks（desktop-release-check）均 completed|success，门禁闭环。剩余开放：#61（待 PR #54 合并，勿动）、#12（P0 服务端轮换密钥，勿代码动）、#72（安全基线：Dependabot/CodeQL/分支保护）、#73/#34（性能，同源可一并收口）。
- 历史（2026-08-05 巡检 run 23）：#74 已修复（P1 评估 / 决策正确性），commit `f26ae33` 已推 `origin/master`，Issue 由 commit 关键字自动关闭（已回帖，comment-5193020272）。根因：#68 只交付了基础设施，**没有任何生产路径调用 `record_decision()`**，`nasdx_decisions.db` 恒空；排查中另发现会让 intraday 永久静默失败的真实 bug——`nasdx/intraday_decision.PositionView` 字段是 `current_price`，而 `record_from_intraday_decision` 读 `last_price`，取价恒 `None`。新增 `nasdx/decision_wiring.py` 作为**唯一**接线层（入口脚本不再直接 import `nasdx.decision_record`）：`record_report_if_enabled`（rules/full，从 `report.performance` 取 provider/model/llm_call_count/total_elapsed_ms 补齐成本侧，否则 `compare_modes` 排名无意义）、`record_intraday_if_enabled`（intraday 是本地确定性规则引擎无 LLM，用 `local`/`intraday_gate` + `nasdx_intraday_decision.v1` 固定占位——空 provider 会让 intraday 静默掉出所有模式/消融对比）、`record_candidate_if_enabled`（portfolio 候选动作串不在内置 action→class 映射，必须显式传 `evaluation_class`），外加 `normalize_data_as_of` 与 `market_snapshot_hash_from_data`（同一次行情刷新共享同一 market hash）。新增 `nasdx/decision_backfill.py` + `run_decision_backfill.py`（只写 `decision_outcomes`，绝不触碰 `decision_records`，重复执行幂等，取价空计 skipped、抛错计 errors）与 `run_decision_evaluation.py`（`--compare/--by-class/--calibration/--ablation` 并入同一报告，样本不足 `verdict=insufficient_sample`、`best_mode=None`）。契约：`NASDX_DECISION_RECORDS=0` 全部 helper 直接返回 None 且**不创建库文件**；所有接线点 fail-open（只打印「决策记录落库跳过」）；`recommendation_review`/`account_review` 输出零改动。`.gitignore` 补 `nasdx_decisions.db`——源码检出时 `get_runtime_dir()` 就是仓库根目录，接线后生产运行会直接在根目录建库。测试 `tests/test_decision_wiring_contracts.py` **19/19** 新增，关联回归 **131/131** 全绿。环境坑：`rm -f` 被 safe-delete 拦截，临时文件清理改用 `python -c "os.remove(...)"`，且不要用 `&&` 把 rm 串在 `git push` 前。剩余开放：#61（待 PR #54 合并，勿动）、#12（P0 服务端轮换密钥，勿代码动）、#71/#72（安全基线）、#73/#34（性能，同源可一并收口）。
- 历史（2026-08-05 巡检 run 22）：#68 已修复（P1 模型有效性 / 过拟合控制 / 产品可信度）。根因：仓库只有「信号是否延续」和「账户实际盈亏」两条复盘链路，**没有任何东西在建议生成的瞬间冻结参考价与 `data_as_of`**，所有事后评价都在用今天的价格重算，天然前视偏差，因此无法回答「14+ 次 LLM 调用比纯规则更准吗」。新增三个模块：`nasdx/decision_record.py`（冻结 `DecisionRecord` dataclass + SQLite insert-only 持久化，同内容幂等、同 id 内容变更直接拒绝，结果标签写独立表 `decision_outcomes` 保证重算标签不污染决策；`rules/full/intraday` 三模式适配器 `record_from_decision_plan` / `record_from_intraday_decision` 写同一套 schema）、`nasdx/outcome_labels.py`（前瞻标签：只读 `data_as_of` 之后的 K 线，交易日以真实 K 线为准而非日历，T+1/3/5/10/20 + MFE/MAE + 最大回撤 + 止损止盈首次触发 + 基准超额，停牌/一字涨跌停/缺失分状态标注）、`nasdx/decision_evaluation.py`（样本量/分布/CI95/盈亏比/最大连亏/置信度分桶校准 + `compare_modes`/`marginal_contribution`/`split_pairs`/`ablation_report`，`assert_no_lookahead` 守卫时间切分）。修复 4 个自身 bug：`stop_loss_pct`/`take_profit_pct` 单位从比率改百分比（与全项目 `*_pct` 约定一致，`_first_trigger` 内除 100）；`marginal_contribution` 的 delta 符号反转（原 `baseline - variant` 会把更优模式判为「可安全关闭」）；`format_evaluation_report` 不渲染扁平结果样本量（违反「先展示样本量再下结论」契约）；无数据早返回时 `horizons` 为空字典致 `KeyError`（补 `_empty_horizons` 占位保持 schema 稳定）。关键决定：`buy/hold` 涨为好、`reduce/avoid` 跌为好，四类语义分开算胜率不合并；首日不可成交默认剔除并计入 `excluded_reasons`；小样本只报样本量与 `insufficient_sample`，不自动选「最佳模式」；隐私沿用既有 `sanitize_text`（只抹凭据形态，不擅自扩大到 PII）。README 新增「三类复盘不是一回事」对照表。测试 `tests/test_decision_evaluation_contracts.py` **56/56** 新增，定向回归 8 套共 **161/161** 全绿。注意：本机 `ACC_PRODUCT_CONFIG_V3` 环境变量超 32767 字符会让 `test_runtime_paths_contracts.py` 报 `ValueError`，与代码无关，需 `env -u ACC_PRODUCT_CONFIG_V3` 运行。剩余开放：#61（待 PR #54 合并，勿动）、#12/#34/#71/#72/#73。
- 历史（2026-08-05 巡检 run 21）：#67 已修复（P1 盘中使用体验/操作一致性/数据新鲜度），commit `070bebd` 已推 `origin/master`，Issue 由 commit message 自动关闭（已回帖修复报告，comment-5184165680）。新增 `nasdx/intraday_decision.py`（版本化 `IntradayDecision` schema + 确定性动作策略，稳定动作枚举非自由文本）、`nasdx/intraday_copilot.py`（账本+行情+规则信号→半小时快照装配与调度，`performance.llm_calls` 恒 0 满足 #65 目标）、`app.py`「🚦 盘中驾驶舱」一级页面、`run_intraday_copilot.py` CLI、`tests/test_intraday_cockpit_contracts.py` **19/19**；`nasdx/rule_based_analysis.py` 加 `technical_signal`/`fund_flow_signal` 公共别名供 copilot 复用规则引擎。关键决定：复用 #66 的 `evaluate_portfolio_gate`/`PortfolioSnapshot` 为组合真相源不重写规则；A 股买入加仓 `_lots_for_budget` 向下取整 100 股整手、不足一手降级「禁止追涨」标「资金不足/不可执行」；fail-closed 优先（缺价/缺快照/硬过期>60min/证据未知→`wait`/`review_required`）；调度仅交易日 09:59…14:59 检查点窗口运行、午休休市跳过；不新增任何自动下单能力（`executable` 仅标记可下单数量，输出定位研究辅助/人工确认）。验收 11/11 全满足；关联回归 104/104（app smoke 3 / gate 33 / link 49）全绿。剩余开放：#61（待 PR #54 合并，勿动）、#68、#12/#34/#71/#72/#73。
- 历史（2026-08-04 巡检 run 19）：#66 端到端接线补齐（P1 组合风险/盘中决策可信度），Issue **保持开放**（本轮只端到端验证了验收 #6/#7/#10，其余待整体复核）。根因：run 17/18 建好了账本→闸门→决策→缓存全链路，但**生产入口从未传 `portfolio=`**——`run_analysis.py` / `analyze.py` / `analyze_batch()` 全走默认 None，线上闸门恒为 `unknown`、盘中缓存看不到任何成交变化，账本形同虚设。新增 `nasdx/portfolio_link.py`（`link_enabled` 开关 + `NASDX_PORTFOLIO_LINK=0` 可关；`ledger_is_initialized()` 只在有事件或现金基线时才算接入，**未初始化不创建库文件**、行为与 #66 之前逐位一致；`market_price_map`/`market_industry_map` 从已加载 data 抽价与板块、脏值丢弃；解析异常返回 fail-closed 快照）；`analyzer.py` 的 `analyze()` 加 `portfolio=` / `link_portfolio=` 覆盖开关，`analyze_batch()` **每批只解析一次**账本并向下传 `link_portfolio=False`，同批共享同一 `portfolio_snapshot_hash`；两个 CLI 入口默认开启接线并加 `--no-portfolio-link` 逃生口。同时修两个 fail-open 缺口：(a) `_portfolio_snapshot_hash()` 只在 `portfolio is None` 时返回空串，接入后异常返回 `portfolio-unresolved`，不再与"未接入"撞缓存键；(b) `portfolio_store._broken_snapshot()` 的 `snapshot_hash` 由空串改为 `sha256("nasdx_portfolio_broken.v1|"+message)`，坏账本不再复用无组合时算出的结论。关键决定：`analyzer.py` 向 `build_decision_plan()` 显式传 `industry=stock_data["sector_name"]`——未持有的新标的没有 `position.industry` 可回落，不传则行业集中度上限对"新增同类标的"**永久失效**（验收 #7）。库层 `NasdxAnalyzer(link_portfolio=False)` 保持默认关，存量调用方零改动。测试 `tests/test_portfolio_link_contracts.py` **41/41** 新增（开关语义/未初始化不建库/fail-closed 哈希非空且随 message 变化/行情映射脏值/缓存哈希非空且随成交变化/入口接线 grep 契约/行业上限对新标的生效/fake-LLM 端到端：接入后缓存写入非空哈希、新成交触发 intraday 重算 risk 维度、关链接回退旧行为），含相邻 8 套定向回归共 **182/182** 全绿。提交用 `refs #66` 不自动关闭。
- 历史（2026-08-04 GitHub 安全/性能审计）：公开仓库有 2 个 fork，二者 head（`3ce6a7c`/`8b366c2`）均为历史清理后当前 master 的祖先；当前 secret alerts 为空、必需密钥扫描 1/1 通过，未发现现行 API Key 明文。残余 P0 是旧 DeepSeek Key 的服务端吊销仍无确认，故重开 #12；当前 scanner 仅扫当前树 `sk-*`，Dependabot/CodeQL/master 保护未启用，新建 #71/#72。性能复核确认 refresh/Stocks60 固定等待下限 29.1s+12s 且重复 30 股，重开 #34；ETF50 quant 仍串行 50 次并固定等待 10s，新建 #73。未改代码/分支/仓库设置；`pip-audit` 依赖解析 120s 超时，无漏洞结论。
- 历史（2026-08-04 巡检 run 18）：#65 已修复（P1 性能/盘中增量分析路径）。新增 `nasdx/analysis_cache.py`（分层缓存基底：硬身份键=文件名 stock_code/provider/model/prompt_version/agent_config_version/cache_schema_version，软失效输入 price/sector/fundamental_fingerprint+risk_profile+portfolio_snapshot_hash+trading_day，逐维度 TTL 300/900/1800/14400s 可 `NASDX_ANALYSIS_TTL_<DIM>` 覆盖，缺失/损坏/版本/身份不符一律 fail-open 到 full，原子写）；`analyzer.py` 接入 `full/intraday/refresh` 三深度——intraday 只跑失效行情维度、复用慢变量与辩论、synthesis 仅在失效维度重算，慢维度未缓存自动回退 full；`battle.py` 5 投票者 `ThreadPoolExecutor` 并发、`VOTERS` 顺序重排、单票失败降级中性；`research.py` 支持 `dimensions=` 增量；`llm.py` 进程级 `_LLMCallCounters` 经 `_LazyLLMClient` 代理计数（注入 `LLMClient._instance` 仍计数）；`schema.py`/`report.py` 加 `analysis_depth`/`freshness`/`performance` 并逐维度标注「本次刷新/复用上一轮」；运行中 `cache/` 目录已加入 `.gitignore` + 5 个 `packaging/windows/*` 脚本排除（不入库、不进便携包/安装包）。关键决定：full 命令/报告格式不变；`intraday` 不声称重核验未刷新维度的公告/基本面/行业信息。测试 `tests/test_analysis_cache_contracts.py` **19/19**（缓存命中/局部失效/完整失效/损坏/并发投票/单票失败/持仓变更/模型版本触发/调用计数与阶段耗时防 14+ 全量回归；第二次 intraday 同指纹 ≤1 LLM 调用、墙钟较 full 减少 ≥70%）；相邻 `test_portfolio_gate_contracts` 33/33 无回归。Issue #65 由 commit message 自动关闭（待 GitHub 回帖）。剩余开放：#61（待 PR #54 合并，勿动）、#66（验收 #6 已随 #65 满足，仍开放待确认）、#67、#68。
- 历史（2026-08-03 巡检 run 17）：#66 验收点 7 已落地（P1 组合风险/盘中决策可信度），Issue **保持开放**（只差 #65 缓存层落地后补一条"持仓变更触发缓存失效"测试）。根因：run 15 只建了权威账本，`build_decision_plan()` / `build_position_sizing()` 仍是纯单票 + 手工汇总金额——账本 fail-closed 时决策层照样能输出"分批布局"，单票看多可以直接覆盖组合层风险。新增 `nasdx/portfolio_gate.py`（确定性 `PortfolioGate`：unknown/normal/no_add/blocked 四态，在单票动作定稿**之前**求值；fail-closed / 单票权重上限 / 行业集中度上限 / 现金<=0 / 满仓 只挡"买入·加仓"，`allow_reduce` 恒为 True——账本坏了不能连减仓都拦）；`decision.py` 加 `portfolio=` / `industry=` 与 `_apply_portfolio_gate()`（挡新增时降级为"维持持仓，不加仓"/"观察等待"/"暂停新增，先修复组合数据"，仓位区间按剩余空间裁剪，输出 `portfolio_snapshot_hash`/`portfolio_version`/`allow_new_position`/`max_new_position_pct`）；`position_sizing.py` 加 `portfolio=`（敞口与现金改从账本 `asset_class_exposure`/`cash` 取，`total_capital` 变可选默认取 `total_assets`，新增金额永不超过账本现金，fail-closed 时全部候选归零）。关键决定：`portfolio=None` 时闸门保持 unknown 且行为与 #66 之前逐位一致，存量调用方（analyzer / rule_based_analysis / app.py / run_final_audit / run_position_sizing）零改动。测试 tests/test_portfolio_gate_contracts 33/33 新增 + 相邻 6 套 89/89 = 122/122。剩余开放：#61（待 PR #54 合并，勿动）、#65、#66（待 #65）、#67、#68。
- 历史（2026-08-03 巡检 run 16）：#70 已修复（P1 数据正确性/组合账本）：correct_event 的修正路径此前绕过整数手守卫——add_trade 拒绝 50 股，但 correct_event 接受 50 股、部分卖出改奇数股、跨代码改科创板 100 股，账本可写入不可成交的成交。新增共享守卫 `_validate_event_against_ledger()`（add_event 与 correct_event 复用同一 board/lot 规则，按 replacement 的 code/side 解析）、`_held_quantity(exclude_event_ids=...)`（修正基线排除被替换原事件）、`_changes_lot_exposure()`（仅修正改变证券/方向/数量时重校验，价格/费用/时间戳修正保持原敞口不重判，避免误拦历史零碎股与清仓）；correct_event 加 `enforce_lot_rules` 参数（事务开启前校验，失败留原事件 active、账本字节一致），replacement 非 TradeEvent 抛 TradeEventError，返回 dict 加 lot_warnings；CLI `correct` 加 `--code/--side/--allow-odd-lot`（高风险逃生口，仍列 lot_warnings）。测试 tests/test_portfolio_ledger_contracts 59/59（+19 TestCorrectionLotRules），相邻隐私套件 33/33，compileall 通过。验收 #1–#9 全满足，Issue 由 commit message 自动关闭（COMPLETED）。剩余开放：#61（待 PR #54 合并，勿动）、#65、#67、#68。
- 历史（2026-08-03 巡检 run 15）：#66 核心账本已落地（P1 持仓正确性/组合风险/盘中决策可信度），commit `e7579a6` 已推 master，Issue **保持开放**（验收 #6 下游消费 #65、#7 决策链接线 #67 未做）。新增 `nasdx/portfolio_store.py`（SQLite 追加事件溯源账本：确定性重放按 occurred_at+side+event_id 排序与本地录入序无关、稳定 snapshot_hash 随任意成交变化、修正 supersede/void 保留审计链、fail-closed 缺价/缺现金/账本损坏、CLI `python -m nasdx.portfolio_store`）+ `nasdx/trade_events.py`（成交事件模型、event_id 幂等键、A股/科创板/北交所/ETF 整数手可配置规则、自由文本脱敏无账号列）；`tests/test_portfolio_ledger_contracts.py` 40/40；`.gitignore` 忽略 `nasdx_portfolio*.db`。验收 #1–#5、#8–#10 本轮回测满足；#6/#7 下游接线留待 #65/#67。剩余开放：#61（待 PR #54 合并，勿动）、#65、#67、#68。
- 历史（2026-08-03 巡检 run 14）：#69 已修复（P1 信息面正确性/时效性），commit `1418263` 已推 master，Issue 由 commit message 自动关闭（COMPLETED）并已回帖完整修复报告。新增消息面证据层三件套：`nasdx/evidence.py`（EvidenceItem 确定性契约：来源类型/权威分/发布与抓取时间/内容指纹/状态；CST 时间归一与盘前盘中盘后判定；转载指纹去重取最早时间+最高权威；按事件类型 TTL；三态 EvidenceBundle verified_new/unverified/unavailable；#65 缓存失效维度信号；买入动作降级 gate；人工证据录入；LLM 摘要只能改 summary 的不可篡改边界；审计视图）、`nasdx/announcement_sources.py`（巨潮资讯公告连接器，session 可注入，分页 + announcementId 跨页去重，ok/empty/failed/parse_failed/not_configured 五态）、`nasdx/news_sources.py`（新闻源归一化与来源分级，默认 not_configured 而非 empty）；`nasdx/external_review.py` 只加 `record_manual_review_evidence()` 桥接，原逻辑与输出不变。关键决定：`evidence_gate()` 中「支撑证据权威分全部低于阈值」的判定必须排在宽泛 `STATE_UNVERIFIED` 早返回**之前**——否则 blocking_evidence_ids 被吞成空元组，社交独占买入拦截失去可追溯性。测试 tests/test_evidence_freshness 77/77 + review_snapshot/architecture 9/9 + plan_table 6/6，compileall 通过。遗留：IntradayDecision 展示待 #67；#65 缓存接线属 #65 范围；仅巨潮一个联网源，其余 not_configured。剩余开放：#61（待 PR #54 合并，勿动）、#65、#66、#67、#68。
- 历史（2026-07-30 巡检 run 10）：#64 已修复（P1 回测正确性/配置安全）：`Backtester` 的执行/成本参数此前只校验 initial_capital，其余直接赋值——slippage=-2 会算出负执行价让恒定价回测净值 10万→30万→70万（凭空造钱），commission_rate=-1 等于全额返现，min_shares=0 下单时才抛 ZeroDivisionError，NaN 滑点跑到 int() 才崩。新增集中式 `validate_backtester_config()` 在 `__init__` fail-fast：费率/滑点必须有限且 0<=x<1，min_shares 正整数（拒 bool/float/str），max_stale_days 非负整数，normalize_weights 严格 bool；类文档写明参数契约表。合法默认值与自定义配置结果逐位不变。测试 tests/test_backtest_correctness_p1 76/76（+17 新用例）。剩余开放：仅 #61（待 PR #54 合并自动关，勿重复动）。
- 历史（run 9）：#63 已修复并关闭（commit f0f30ab）：决策记忆改 NASDX_MEMORY_ENABLED opt-in，关闭时 recall 返回 []；summary 复用 sanitize_text() 脱敏 + 截断；NASDX_MEMORY_MAX_RECORDS 条数淘汰；clear_memory/memory_status + CLI。测试 test_memory_privacy 19/19。
- 历史（run 8）：#62 已修复并关闭（commit 54a1c53 已推送 master）：决策日志改 opt-in 默认关闭（NASDX_DECISION_LOG=1 显式开启）；写盘前递归脱敏（敏感键→[REDACTED]，字符串内 Bearer/sk-/key=value 抹除，可用 NASDX_DECISION_LOG_REDACT_KEYS 扩展）；弃用 repr()（未知对象只记类型摘要）；5MB 单备份轮转。测试 tests/test_decision_log_privacy.py 14/14 + 存量契约 28/28。剩余开放：#61（待 PR #54 合并自动关）、#63（P2 决策记忆保留策略，下轮建议）。
- 历史（run 7）：#61 已修复（commit 07f32d4，落在 PR #54 分支 feat/audit-harness——audit_loop.py 只存在于该分支，master 无此文件）：phase_fix 不再在 PR 创建时关 Issue；新增 phase_verify（PR 合并+merge commit 可达默认分支才标 fixed+关闭；closed-unmerged 清状态可重试并重开误关 Issue）；_apply_and_test 全链路 git 返回码检查；状态 prs/fixed 分层+旧 schema 自动迁移。测试 tests/test_audit_loop_lifecycle.py 23/23（全 mock gh/git）。PR #54 body 已挂 Closes #61，#61 保持开放待 PR 合并。剩余开放：#62、#63（P2）、PR #53/#54。run 6 已补账推送 6f06815/0c93c4f/d30f734 并关闭 #43/#45/#48。
- 历史（run 5）：#45 已修复（fail-fast 权重校验 + opt-in 归一化），本地提交 0c93c4f；连同上轮 6f06815（#43 引擎级重复时间戳测试）共 2 个提交**待推送**——本机到 GitHub 全路径网络阻断（443/22 均被断，fake-IP 198.18.x 连接即关），恢复后先 `git push origin master`，再到 #43/#45 回帖修复报告并关闭。
- 测试：tests/test_backtest_correctness_p1 51/51 通过；存量失败仅 test_ohlcv_standardization_from_chinese_columns（环境 dtype，与改动无关）。
- 关键决定（#45）：validation 与 normalization 分层——默认 WeightValidationError fail-fast（NaN/inf/bool/负值/未知标的/单权重>1/Σ>1+1e-6 容差），归一化仅 `Backtester(normalize_weights=True)` 显式 opt-in 并记诊断；买入顺序 (-权重, 代码) 确定性排序；新增 result.weight_allocations 暴露 requested vs executed。

## 历史（前一阶段）
- GitHub issue #27 已通过 CI、回复并关闭；issue #28 的 Node 20 action 弃用警告也已修复并通过无注解 CI，待文档收口、回复和关闭。
- 上次停在：`.github/workflows/windows-desktop.yml` 已升级到 `actions/checkout@v5` 和 `actions/setup-python@v6`；pytest 210/210、最终审计 22/22、Windows CI 均通过，check-run annotations 为 `[]`。
- 关键决定：只升级官方 action 主版本，保留 `windows-latest`、Python 3.11.9、密钥扫描、依赖锁校验和桌面发布命令不变。
- 原因：#27 的成功 CI run 仍提示 Node.js 20 已弃用，并由 GitHub 强制转换到 Node 24；官方 Node 24 action 已消除兼容路径和注解噪音。
- 前序证据：Streamlit 1.59.2 下 `/?page=plan` 在 1440x900 和 390x844 正常渲染，控制台均为 0 errors / 0 warnings；pytest 210/210、最终审计 22/22、桌面发布 11/11。

## Desktop Packaging

- 当前：smoke 脚本会在收尾清理包内 `__pycache__/`、`*.pyc`、`*.pyo`；`run_desktop_release_check.py` 会在 zip/installer 输入之后再汇总 release evidence。
- 关键决定：保留 smoke 对真实包/安装布局的启动验证，但验证结束后把由 Python 运行生成的缓存从 `{app}`/portable 包内移除。
- 原因：正式包不能因为验证动作本身留下禁入缓存；release evidence 必须检查本轮刚生成并 smoke 过的产物。

## 文档维护（2026-08-04）
- README.md 已美化精简：信息过载（33 条 emoji 功能 / 70+ 文件架构树 / 400+ 行命令）。重写为干净版本，核心命令保留，开发/打包/缓存/组合细节折叠进 `<details>` 或外链 `docs/` 下对应文档。维护约定：长命令不再堆回主文档。
